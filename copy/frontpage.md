# Front page copy

EDITOR OWNS THIS FILE. Design renders it and writes none of it.

Everything down to the first `##` heading is notes, and the renderer never
reads it, so guidance can live here without any risk of reaching a page.
The blocks below are the only thing that renders.

## The lede

Leave the `lede` block EMPTY to fall back to the generated three-clause
line. Write one sentence in it to lead the page yourself.

The lede is yours most weeks, and one sentence is the whole job. Product's
ruling, 2026-08-11: a machine cannot be punchy here without ranking, and a
human can. Every rule design proposed for picking a channel to lead with
compares different units, so it selects the smallest unit rather than the
biggest story, and a stated rule makes that choice predictable rather than
neutral. Leading with something is itself the claim.

So the generated form is the FALLBACK for a week nobody writes one, and on
that week calm and concrete is the correct output. It is not the normal
case and should not read as one.

Two things the generated fallback holds that a written lede must hold too:

- No comparison between channels. Each figure is measured against its own
  place's own record, and none of them share a scale.
- "At least" on any heat count. The counts are floors under D-112, and
  dropping the qualifier for rhythm makes it a claim we cannot support.

## Editor-owned prose that is NOT in this file

**Read this before concluding you have nothing live on the front page.**
Any clause carrying an assembled figure cannot be static text here, so it
lives in the renderer instead. It is still yours: design renders it and
did not write it.

Known, 2026-09-04:

    templates/frontpage.py:1054   ", against an average of %d"
                                  the crop calibration clause, mine,
                                  live since 2026-08-30

**This file is therefore not a complete inventory of editor copy on the
front page, and treating it as one is how a live line gets forgotten.** I
did exactly that: asked two chats whether the calibration line was still
owed, twice, while it had been published for five days. `copy/` had no
record and I took the absence as the answer.

**Add a line here whenever a clause of yours goes into the renderer.**
The rule that catches this generally is in editorial standards section 1:
verify absence per store, because one store's silence is not the set.

## lede

**Paris has had 15 times more hot days this summer than its 1961-1990
average.**
